job_id: peek.clients
version: '2.0'
steps:
- step_id: read
  op: storacle.query
  params:
    dataset: derived
    table: proj_clients
    columns: ['*']
    filters: '@payload.filters'
    limit: '@payload.limit'
    order_by: '@payload.order_by'
