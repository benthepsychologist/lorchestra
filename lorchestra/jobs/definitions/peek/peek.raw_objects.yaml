job_id: peek.raw_objects
version: '2.0'
steps:
- step_id: read
  op: storacle.query
  params:
    dataset: raw
    table: raw_objects
    columns: ['*']
    filters: '@payload.filters'
    limit: '@payload.limit'
    order_by: '@payload.order_by'
