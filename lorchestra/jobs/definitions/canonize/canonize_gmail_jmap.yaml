job_id: canonize_gmail_jmap
version: '2.0'
steps:
- step_id: canonize
  op: call
  params:
    callable: canonizer
    source_type: gmail
    config:
      transform_id: email/gmail_to_jmap_lite@1.1.0
- step_id: persist
  op: plan.build
  params:
    items: '@run.canonize.items'
    method: wal.append
- step_id: write
  op: storacle.submit
  params:
    plan: '@run.persist.plan'
