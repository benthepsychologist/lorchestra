job_id: canonize_stripe_payment_intents
version: '1.0'
steps:
- step_id: canonize
  op: call.canonizer
  params:
    source_type: stripe
    config:
      transform_id: payment/stripe_to_canonical@1-0-0
