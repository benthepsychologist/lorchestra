job_id: ingest_exchange_booking_mensio
version: '2.0'
steps:
- step_id: ingest
  op: call
  params:
    callable: injest
    source: exchange_booking_mensio
- step_id: persist
  op: plan.build
  params:
    items: '@run.ingest.items'
    method: bq.upsert
    dataset: raw
    table: raw_objects
    key_columns:
    - idem_key
    payload_wrap: true
    id_field: id
    field_defaults:
      source_system: exchange
      connection_name: exchange-booking-mensio
      object_type: email
- step_id: write
  op: storacle.submit
  params:
    plan: '@run.persist.plan'
